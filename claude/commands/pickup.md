---
description: Resume work saved by /handoff (reads .claude/handoff.md)
---

Read `.claude/handoff.md` in the current project and resume the work exactly as it
directs: open the spec/plan file it references, re-establish where things stand,
and continue from the stated next step — using `superpowers:subagent-driven-development`
for any implementation.

If `.claude/handoff.md` doesn't exist, tell me there's nothing to resume and stop.

> **Related:** `/pickup` continues a single handed-off task. To see the whole-session
> checklist from `/wrap-up`, use `/todo` instead.
