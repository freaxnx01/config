---
description: Execute the current implementation plan subagent-driven
---

Execute the current implementation plan using the
`superpowers:subagent-driven-development` skill — dispatch the plan's independent
tasks to subagents rather than implementing inline.

If no plan is loaded in context, ask me which spec/plan file to read first, then
proceed.
